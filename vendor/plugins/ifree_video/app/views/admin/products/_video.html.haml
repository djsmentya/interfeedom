%fieldset.inputs{:name => "video"}
  %legend
    %span Video
  %ol
    %li#product_genre.string.required
      %label{:for => "product_genre"} Genre
      =select_tag 'product[genre_id]',options_from_collection_for_select(Genre.all, 'id','name')
    %li  
      %label{:for => "product_producer"} Producer
      %ol
        %li
          =text_field_tag 'product[producer]'
-#= semantic_fields_for :product, nil,:builder do |f|
  = f.inputs 'Video Product' do
    = f.input :genre_id, :collection => Genre.all
    = f.input :producerdd
