= semantic_form_for :product, :url => {:controller => 'admin/products',:action => :create} do |f|
  = f.inputs 'Video Product' do
    = f.input :product_type_id, :collection => ProductType.all
    = f.input :name
    = f.input :description, :as =>:text
    = f.input :genre
    = f.input :producer
    = f.input :image, :as => :file
  = f.buttons
-#:javascript
  $(document).ready(function() {
    $('#product_product_type_id').change(loadProductEdit);
  });

  function loadProductEdit() {
    var type = $('#product_product_type_id option:selected').text().toLowerCase();
    $('#main_content').load('new_product?product_type=' + type);
  }
