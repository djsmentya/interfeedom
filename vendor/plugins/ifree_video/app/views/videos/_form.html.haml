= form_for @property, :url=> {:controller => 'videos', :action => 'update'} , :method => :put do |f|
  -if @property.errors.any?
    #error_explanation
      %h2= "#{pluralize(@video.errors.count, "error")} prohibited this video from being saved:"
      %ul
        - @video.errors.full_messages.each do |msg|
          %li= msg

  .field
    -#= f.label :genre
    = f.text_field :value
  .field
    -#= f.label :producers
    -#= f.text_field :producers
  -#.field
  -#  = f.label :completion_year
  -#  = f.date_select :completion_year
  %label
    'genre'
  = text_field_tag :genre,  @video.property.value[:genre]
  = text_field_tag :duu,  @video.property.value[:genre]

  .actions
    = f.submit 'Save'
