= form_for @profile_order do |f|
  -if @profile_order.errors.any?
    #error_explanation
      %h2= "#{pluralize(@profile_order.errors.count, "error")} prohibited this profile_order from being saved:"
      %ul
        - @profile_order.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
